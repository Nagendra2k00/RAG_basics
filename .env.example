# Optional: copy to .env and adjust as needed.
# Defaults work with local Ollama (nomic-embed-text).

# Embedding provider (supported: ollama)
# EMBEDDING_PROVIDER=ollama

# Ollama embedding model name (must be pulled: ollama pull nomic-embed-text)
# OLLAMA_EMBEDDING_MODEL=nomic-embed-text
